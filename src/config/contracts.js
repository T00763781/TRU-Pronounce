// Phase 0: placeholders only.
// Do not wire these into runtime logic yet.
//
// `LIBRARY_INDEX_URL` will point at the Runtime Library repo's `index.json`
// (either a raw GitHub URL or GitHub Pages URL).
//
// `SUBMISSION_ENDPOINT_URL` is a placeholder for a future submission interface
// (could be implemented later via issues/PRs/worker/etc.). No network calls in Phase 0.

export const LIBRARY_INDEX_URL = "TO_BE_SET";
export const SUBMISSION_ENDPOINT_URL = "TO_BE_SET";

